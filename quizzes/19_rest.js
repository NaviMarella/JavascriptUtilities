// Task 1

let str = ['JavaScript', 'Rest', 'Parameters']
// Create a concat method to add all these strings using rest parameters

// Task 2

function sortArguments() {
    var sortedArgs = arguments.sort();
    return sortedArgs; // this will never happen, why??
}


console.log(sortArguments(5, 3, 7, 1));// What is the output -- Try to execute and see??